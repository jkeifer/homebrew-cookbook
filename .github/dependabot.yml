version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    # Batch all action bumps into a single PR.
    groups:
      github-actions:
        patterns:
          - "*"
    commit-message:
      prefix: "ci"
